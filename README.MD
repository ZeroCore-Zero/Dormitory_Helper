# IMPORTANT

**Can run but with large number of BUGs.**(so sad...)

# Use Toturial

Backend:
```bash
    pip install -r requirements.txt
    python app.py
```

Frontend:
```bash
    cd ./frontend
    npm install
    npm run dev
```